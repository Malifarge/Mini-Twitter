import { StyleSheet } from 'react-native'

const globalStyles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    marginTop: 60,
    flex: 1
  },
  titre: {
    fontSize: 50
  }
})

export default globalStyles